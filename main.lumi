fun main() {
    let i = 1
    while i <= 10 {
        println(factorial(i))
        i = i + 1
    }
}

fun factorial(n int) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}
