fun main() {
    let n = 10
    println(fibonacci(n))
}

fun fibonacci(n int) {
    let a = 0, b = 1

    let i = 0
    while i < n {
        let tmp = a + b
        b = a
        a = tmp

        i = i + 1
    }

    return a
}
