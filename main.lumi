fun main() {
    for let i = 1; i <= 10; i += 1 {
        let fib = fibonacci(i)
        printf("fib(%d): %d\n", i, fib)
    }
}

fun fibonacci(n int) {
    let a = 0, b = 1

    for let i = 0; i < n; i += 1 {
        let tmp = a + b
        b = a
        a = tmp
    }

    return a
}
