fun main() {
    for let i = 1; i <= 10; i += 1 {
        printf("2^%d = %d\n", i, power(2, i))
    }
}

fun power(base int, exp int) {
    let result = 1
    for let i = 0; i < exp; i += 1 {
        result *= base
    }
    return result
}
