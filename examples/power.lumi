fun main() {
    for i in 1..=10 {
        printf("2^%d = %d\n", i, power(2, i))
    }
}

fun power(base int, exp int) {
    let result = 1
    for i in 0..exp {
        result *= base
    }
    return result
}
