fun main() {
    for i in 1..=10 {
        let fib = fibonacci(i)
        printf("fib(%d): %d\n", i, fib)
    }
}

fun fibonacci(n int) {
    let a = 0, b = 1

    for i in 0..n {
        let tmp = a + b
        b = a
        a = tmp
    }

    return a
}
