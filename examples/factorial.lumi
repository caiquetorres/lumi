fun main() {
    let i = 1
    while i <= 10 {
        let fac = factorial(i)
        printf("fac(%d): %d\n", i, fac)
        i += 1
    }
}

fun factorial(n int) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}
