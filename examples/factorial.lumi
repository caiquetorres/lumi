fun main() {
    for let i = 1; i <= 10; i += 1 {
        let fac = factorial(i)
        printf("fac(%d): %d\n", i, fac)
    }
}

fun factorial(n int) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}
