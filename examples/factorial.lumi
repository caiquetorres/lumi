fun main() {
    for i in 1..=10 {
        let fac = factorial(i)
        printf("fac(%d): %d\n", i, fac)
    }
}

fun factorial(n int) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}
